spring.application.name=spring-boot-course
server.port=8082
spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017

# another way
#spring.data.mongodb.uri=mongodb://localhost/${COLLECTION_NAME}

# connecting mongo atlas
#spring.data.mongodb.uri=${MONGO_URI}

# not Recommended
#spring.security.user.name=${USERNAME}
#spring.security.user.password=${PASSWORD}
secret.key=${SECRET_KEY}
spring.main.allow-circular-references=true

spring.data.mongodb.database=SpringCourse

spring.data.mongodb.auto-index-creation=true
weather.api.key=${WEATHER_API_KEY}

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${GMAIL_USERNAME}
spring.mail.password=${GMAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
#spring.data.redis.host=localhost
#spring.data.redis.port=6379


#spring.data.mongodb.username=${DB_USER_NAME}
#spring.data.mongodb.password=${DB_PASSWORD}
#logging.level.com.spring.boot.course.SpringBootCourseApplication = trace

# Kafka Cloud

# Required connection configs for Kafka producer, consumer, and admin
#spring.kafka.properties.sasl.mechanism=PLAIN
#spring.kafka.bootstrap-servers=${KAFKA_SERVER}
#spring.kafka.properties.sasl.jaas.config=org.apache.kafka.common.security.plain.PlainLoginModule required username='${KAFKA_USERNAME}' password='${KAFKA_PASSWORD}';
#spring.kafka.properties.security.protocol=SASL_SSL
#
## Best practice for higher availability in Apache Kafka clients prior to 3.0
#spring.kafka.properties.session.timeout.ms=45000
#
#client.id=${KAFKA_CLIENT_ID}
